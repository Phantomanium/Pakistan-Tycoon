# URL of the Pakistan Tycoon multiplayer server (Socket.IO).
# Leave unset for local dev — the client defaults to http://localhost:3001.
# In production (e.g. on Netlify), set this to your deployed server's URL:
#   VITE_SERVER_URL=https://your-server.onrender.com
VITE_SERVER_URL=
